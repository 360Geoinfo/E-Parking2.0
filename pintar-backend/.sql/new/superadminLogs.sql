CREATE TABLE
    "SMARTPARKINGDB"."SUPERADMIN_LOGS" (
        "ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
        "SUPERADMIN_ID" VARCHAR2 (250 BYTE),
        "ACTION" VARCHAR2 (50 BYTE),
        "DETAILS" CLOB,
        "TIMESTAMP" TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

COMMIT;