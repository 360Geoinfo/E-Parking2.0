CREATE TABLE
    "SMARTPARKINGDB"."TRANSACTIONS" (
        "ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
        "TRANSACTIONID" VARCHAR2 (250 BYTE) UNIQUE,
        "TYPE" VARCHAR2 (50 BYTE), -- SAMAN, PAYMENT_TO_OPERATOR, PETAK, SEASONAL,
        "REFID" VARCHAR2 (250 BYTE) -- SAMAN ID, PAYMENT_TO_OPERATOR ID, PETAK ID, SEASONAL ID
    );